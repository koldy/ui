/**
 * Idea:
 *
 * <Responsive from="xs" to="sm">
 *   {({isXs, isMd, isLg, innerWidth, innerHeight}) => (
 *     <Flexbox alignItems={isXs ? 'flex-start' : 'center'}>
 *       <Flexbox.Item></Flexbox.Item>
 *       <Flexbox.Item></Flexbox.Item>
 *       <Flexbox.Item></Flexbox.Item>
 *     </Flexbox>
 *   )}
 * </Responsive>
 */
